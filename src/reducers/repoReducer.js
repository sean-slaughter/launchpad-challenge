
const initialState = {
    state: "WORKING"
}

const repoReducer = (state = initialState, action) =>{
    switch(action.type){
        default:
            return state;
    }
}

export default repoReducer